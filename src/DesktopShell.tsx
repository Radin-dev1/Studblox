import {useState} from 'react';
import {BracketsCurly,ChatCircleDots,Desktop,SignIn,UserCircle} from 'phosphor-react';
import {App} from './App';
import {Workbench} from './Workbench';
const accountUrl='https://radin-dev1.github.io/Studblox/';
export function DesktopShell(){const [guest,setGuest]=useState(false),[studioMode,setStudioMode]=useState(true);if(!guest)return <main className="guest-gate"><div className="guest-brand"><span>S</span>Stud Blox</div><section><div className="desktop-orbit"><Desktop/></div><p>DESKTOP WORKSPACE</p><h1>Build with an account<br/>or continue as a guest.</h1><span>Guest mode keeps your work and provider settings on this computer. Sign in when you want account-backed sync and shared settings.</span><div><button onClick={()=>setGuest(true)}><UserCircle/>Continue as guest</button><button onClick={()=>window.open(accountUrl,'_blank')}><SignIn/>Sign in in browser</button></div><small>Guest mode does not upload projects or create a cloud identity.</small></section></main>;return <div className="mono-app"><App/><button className="guest-status" onClick={()=>setGuest(false)}><UserCircle/>Guest <span>local only</span></button><button className="workspace-switch" onClick={()=>setStudioMode(v=>!v)}>{studioMode?<><ChatCircleDots/>Agent chat</>:<><BracketsCurly/>Studio tools</>}</button>{studioMode&&<div className="workbench-overlay"><Workbench/></div>}</div>}
